[project]
name = "cli"
version = "0.1.0"
requires-python = ">=3.14"

[project.optional-dependencies]
dev = [
	"ty~=0.0.29",
	"ruff~=0.15.8",
]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "RUF"]
ignore = ["E501", "F401"]
